node_modules/
dist/
.DS_Store
*.log
# Local-only settings such as the MAME ROM path. Never commit ROMs or the Atari source.
local.config.json
# Decoded from a local copy of the original's tables; never commit (ADR 0001).
src/data/local/

# VitePress build output and cache.
docs/.vitepress/dist/
docs/.vitepress/cache/
