import type { GameState, Input } from '../types';

/** The Dogfight: open space against TIE Fighters and Darth's Ship. Skeleton only. */
export function enterDogfight(_state: GameState): void {}

export function stepDogfight(_state: GameState, _input: Input, _dt: number): void {}
