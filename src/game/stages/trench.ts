import type { GameState, Input } from '../types';

/** The Trench: past Catwalks and Trench Turrets to the Exhaust Port. Skeleton only. */
export function enterTrench(_state: GameState): void {}

export function stepTrench(_state: GameState, _input: Input, _dt: number): void {}
