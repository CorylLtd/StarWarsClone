import type { GameState, Input } from '../types';

/** The Surface: over the Death Star among Laser Towers and Laser Bunkers. Skeleton only. */
export function enterSurface(_state: GameState): void {}

export function stepSurface(_state: GameState, _input: Input, _dt: number): void {}
